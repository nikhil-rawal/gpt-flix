specVersion: "v1beta"
serviceId: "gpt-flix"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "gpt-flix-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
